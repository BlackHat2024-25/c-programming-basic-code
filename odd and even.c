// 7.	WAP in C to check whether the number is odd or even.

#include<stdio.h>

float main(){
float num;

printf("Enter your number :");
scanf("%f",&num);

(num / 2==0) ? printf("%f is even number",num) : printf("%f is a odd number",num);
return 0;

}
